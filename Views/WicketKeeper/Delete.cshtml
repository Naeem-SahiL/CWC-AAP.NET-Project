@model core.Models.WicketKeeperModel

@{
    ViewData["Title"] = "Delete wicket keeper record";
    Layout = null;
}

<h3 class="text-center">@ViewData["Title"]</h3>
<div>
    <div class="container">
        <div class="col-108 mx-auto">
            <h5 class="text-danger text-center">Are you sure you want to delete this?</h5>
            <hr />
            <dl class="row">
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.wk_name)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.wk_name)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.wk_rank)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.wk_rank)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.no_of_catches)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.no_of_catches)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.no_of_stumps)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.no_of_stumps)
                </dd>
            </dl>
            <form asp-action="Delete" asp-route-id="@Model.wk_id" onsubmit="return ajaxPost(this);">
                <input type="hidden" asp-for="wk_id" />

                <input type="submit" value="Delete" class="btn btn-danger btn-block" /><br />

            </form>
        </div>
    </div>
</div>
