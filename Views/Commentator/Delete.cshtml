
@model core.Models.CommentatorModel

@{
    ViewData["Title"] = "Delete commentator record";
    Layout = null;
}

<h3 class="text-center">@ViewData["Title"]</h3>
<div>
    <div class="container">
        <div class="col-108 mx-auto">
            <h5 class="text-danger text-center">Are you sure you want to delete this?</h5>
            <hr />
            <dl class="row">
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.name)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.name)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.commentator_matches)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.commentator_matches)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.country)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.country)
                </dd>
            </dl>
            <form asp-action="Delete" asp-route-id="@Model.commentator_id" onsubmit="return ajaxPost(this);">
                <input type="hidden" asp-for="commentator_id" />

                <input type="submit" value="Delete" class="btn btn-danger btn-block" /><br />

            </form>
        </div>
    </div>
</div>
