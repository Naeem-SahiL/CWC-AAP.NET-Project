@model core.Models.UmpiresViewModel

@{
    ViewData["Title"] = "Delete Umpire record";
    Layout = null;
}

<h3 class="text-center">@ViewData["Title"]</h3>
<div>
    <div class="container">
        <div class="col-108 mx-auto">
            <h5 class="text-danger text-center">Are you sure you want to delete this?</h5>
            <hr />
            <dl class="row">
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.umpire_name)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.umpire_name)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.maches_umpired)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.maches_umpired)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.country)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.country)
                </dd>
            </dl>
            <form asp-action="Delete" asp-route-id="@Model.umpire_id" onsubmit="return ajaxPost(this);">
                <input type="hidden" asp-for="umpire_id" />

                <input type="submit" value="Delete" class="btn btn-danger btn-block" /><br />

            </form>
        </div>
    </div>
</div>
