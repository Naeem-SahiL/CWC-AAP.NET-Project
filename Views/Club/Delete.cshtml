@model core.Models.ClubViewModel

@{
    ViewData["Title"] = "Delete club record";
    Layout = null;
}

<h3 class="text-center">@ViewData["Title"]</h3>
<div>
    <div class="container">
        <div class="col-108 mx-auto">
            <h5 class="text-danger text-center">Are you sure you want to delete this?</h5>
            <hr />
            <dl class="row">
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.club_name)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.club_name)
                </dd>
                <dt class="col-sm-6">
                    @Html.DisplayNameFor(model => model.country)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.country)
                </dd>
                
            </dl>
            <form asp-action="Delete" asp-route-id="@Model.club_id" onsubmit="return ajaxPost(this);">
                <input type="hidden" asp-for="club_id" />

                <input type="submit" value="Delete" class="btn btn-danger btn-block" /><br />

            </form>
        </div>
    </div>
</div>
